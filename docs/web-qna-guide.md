# 웹 설명 모범 답안

이 문서는 `거짓 변명`용이 아니라, `사실 기반으로 어떻게 설명할지`를 정리한 답변집입니다.

내 상황의 핵심은 이렇습니다.

- 나는 원래 HTML도 거의 모르는 수준이었다
- 그래서 웹은 혼자 처음부터 다 짜기보다 AI를 `튜터/보조 도구`처럼 썼다
- 단순 복붙으로 끝낸 것이 아니라, 구조와 동작을 설명받으면서 이해 가능한 수준으로 정리했다
- 지금은 메인 구조, 검색 흐름, 추가/수정/삭제, 상세 팝업, localStorage, GitHub Pages 정도는 설명할 수 있다

이 문서의 목표는 2가지입니다.

1. 짧게 질문받았을 때 바로 답하기
2. "설명해봐"라고 들어왔을 때 이어서 자연스럽게 풀어 말하기

---

## 1. 공통 원칙

항상 아래 방향으로 말합니다.

- `웹은 처음이었다`
- `그래서 AI를 튜터처럼 활용했다`
- `구조를 이해하면서 구현했다`
- `지금은 핵심 흐름은 설명할 수 있다`

피해야 하는 말은 아래입니다.

- `제가 처음부터 전부 다 짰어요`
- `AI는 거의 안 썼어요`
- `사실 잘 모르는데 그냥 나왔어요`

좋은 답변은 `모른 상태 -> 배운 방식 -> 지금 설명 가능한 범위` 순서로 말합니다.

---

## 2. 제일 먼저 쓸 수 있는 기본 답변

### 2-1. 팀원에게 짧게 말할 때

`나는 원래 HTML도 거의 모르는 상태였어. 그래서 웹은 AI를 튜터처럼 써서 구조 설명을 받고, 하나씩 이해하면서 만들었어. 그냥 복붙으로 끝낸 게 아니라, 왜 섹션이 나뉘는지랑 검색이나 수정이 어떻게 도는지 계속 설명받으면서 맞췄어.`

### 2-2. 교수님께 짧게 말할 때

`저는 웹 개발 경험이 거의 없어서 HTML도 기초부터 배우는 수준이었습니다. 그래서 이번 웹 파트는 AI를 보조 도구로 활용해 구조와 동작 원리를 설명받으면서 진행했습니다. 단순 생성에 그치지 않고, 화면 구성과 기능 흐름을 이해 가능한 형태로 정리하면서 프로토타입을 만들었습니다.`

### 2-3. 더 짧게 한 줄로 말할 때

`웹은 처음이라 AI를 튜터처럼 활용하면서 구조를 배우고, 그걸 바탕으로 프로토타입을 정리했습니다.`

---

## 3. "네가 직접 한 게 뭐냐"에 대한 답변

### 짧은 답변

`혼자 바로 구현하기 어려운 부분은 도움을 받았지만, 제가 맡은 건 웹 화면 구조를 잡고, 검색/추가/수정/삭제 같은 기본 사용자 흐름이 동작하도록 정리한 부분입니다.`

### 조금 더 자세한 답변

`웹은 처음이라 처음부터 다 아는 상태는 아니었습니다. 대신 화면을 어떤 섹션으로 나눌지, 검색 폼을 어떻게 둘지, 모임 추가와 수정은 같은 폼으로 어떻게 처리할지, 상세 정보는 팝업으로 어떻게 뺄지 같은 흐름은 계속 설명받으면서 정리했습니다. 배포까지 연결해서 실제로 열리는 결과물로 만든 것도 제가 챙긴 부분입니다.`

---

## 4. "설명해봐"가 들어오면 쓰는 기본 구조

이 질문이 나오면 무조건 아래 순서로 말하면 됩니다.

1. `역할`
2. `왜 그렇게 했는지`
3. `어떻게 동작하는지`

이걸 외워두면 대부분 버틸 수 있습니다.

예시:

`이 검색 부분은 원하는 모임을 조건으로 찾는 역할입니다. 왜 이렇게 했냐면, 나중에 DB를 붙이기 전에도 사용자 흐름을 먼저 확인하려고 했기 때문입니다. 지금은 사용자가 키워드, 카테고리, 지역, 상태를 고르면 JavaScript가 배열 데이터를 필터링해서 다시 화면에 뿌리는 방식입니다.`

즉, 항상

`이건 무엇이다 -> 왜 필요하다 -> 지금은 어떻게 돈다`

이 흐름으로 말합니다.

---

## 5. 핵심 기능 설명 모범 답안

아래는 실제로 설명해보라고 했을 때 쓸 수 있는 답변입니다.

### 5-1. 전체 구조 설명

`전체는 HTML, CSS, JavaScript를 나눠서 구성했습니다. HTML은 화면 구조를 만들고, CSS는 색감과 레이아웃 같은 디자인을 담당하고, JavaScript는 검색, 추가, 수정, 삭제, 팝업 같은 동작을 담당합니다. 이렇게 나누면 나중에 수정할 때도 역할이 분리돼서 보기 쉽습니다.`

### 5-2. 메인 페이지 구조 설명

`메인 페이지는 위에서부터 소개 화면, 기능 소개, 검색, 지도 자리, 내 모임, 내가 만든 모임, 모임 생성 폼 순서입니다. 처음 보는 사람이 서비스 목적을 먼저 이해하고, 그 다음 바로 모임을 찾거나 만들 수 있게 흐름을 잡은 구조입니다.`

### 5-3. 검색 기능 설명

`검색은 입력값을 받아서 현재 모임 배열에서 조건에 맞는 것만 골라내는 방식입니다. 키워드, 카테고리, 지역, 상태를 읽고, 맞는 데이터만 필터링한 뒤 다시 목록을 그립니다. 즉 서버 검색은 아니고, 프론트엔드 단계에서 흐름을 먼저 구현한 것입니다.`

### 5-4. 정렬 기능 설명

`정렬은 필터링된 결과를 다시 날짜순, 남은 자리순, 인기순으로 재정렬하는 방식입니다. 조건 검색과 정렬을 분리해 두면 나중에 백엔드로 넘어가도 개념이 이어집니다.`

### 5-5. 모임 추가 기능 설명

`모임 추가는 폼에 입력한 값을 읽어 새 객체로 만들고, 그 데이터를 목록 맨 앞에 넣은 뒤 저장하고 다시 렌더링하는 흐름입니다. 즉 입력 -> 데이터 생성 -> 저장 -> 화면 갱신 순서입니다.`

### 5-6. 수정 기능 설명

`수정은 새 폼을 따로 만든 게 아니라, 같은 폼을 재사용했습니다. 수정 버튼을 누르면 기존 값이 폼에 다시 채워지고, 저장할 때는 새로 추가하는 게 아니라 해당 id의 데이터를 덮어쓰는 방식입니다. 그래서 추가 모드와 수정 모드를 하나의 폼으로 처리할 수 있습니다.`

### 5-7. 삭제 기능 설명

`삭제는 실수를 막기 위해 바로 지우지 않고 confirm으로 한 번 확인합니다. 확인하면 배열에서 해당 데이터를 빼고, 저장한 뒤 화면을 다시 그립니다.`

### 5-8. 상세 팝업 설명

`카드 안에 모든 정보를 다 넣으면 복잡해지기 때문에, 카드에는 요약만 두고 상세 내용은 dialog 팝업으로 분리했습니다. 상세 보기 버튼을 누르면 선택한 모임 데이터를 찾아서 제목, 날짜, 설명 등을 팝업 안에 넣고 열어주는 방식입니다.`

### 5-9. localStorage 설명

`처음에는 새로고침하면 추가한 모임이 다 사라졌습니다. 그래서 브라우저 localStorage를 써서 데이터를 저장하게 했습니다. 지금은 모임 데이터가 바뀔 때마다 문자열로 저장하고, 페이지가 다시 열리면 그 저장값을 읽어와서 이어서 보여줍니다.`

### 5-10. 왜 `mine`과 `createdByMe`를 나눴는지 설명

`참여한 모임과 내가 직접 만든 모임은 다르기 때문에 값을 나눴습니다. mine은 내가 참여했는지를 뜻하고, createdByMe는 내가 직접 만든 모임인지를 뜻합니다. 이렇게 나눠야 수정/삭제 권한을 정확히 관리할 수 있습니다.`

### 5-11. 로그인 페이지 설명

`로그인 페이지는 메인과 역할이 달라서 별도 페이지로 분리했습니다. 지금 단계는 실제 인증이 아니라 디자인과 입력 흐름을 먼저 만드는 단계라서, 비밀번호 보기/숨기기와 버튼 반응만 넣은 정적 데모 수준입니다. 실제 로그인 API는 아직 붙이지 않았습니다.`

### 5-12. 왜 API를 아직 안 붙였는지 설명

`현재는 계획서 수준에 맞게 프론트엔드 뼈대와 사용자 흐름을 먼저 완성하는 단계입니다. 지도 API, 실제 로그인, 결제는 다음 단계로 분리해 두고, 지금은 먼저 화면 구조와 동작 흐름을 안정화하는 데 집중했습니다.`

### 5-13. 배포 설명

`정적 웹 프로젝트라 GitHub Pages로 배포했습니다. 저장소에 HTML, CSS, JavaScript 파일을 올리고, GitHub Pages 설정으로 웹사이트처럼 열리게 한 것입니다. 그래서 링크만 있으면 바로 결과물을 보여줄 수 있습니다.`

---

## 6. 질문별 모범 답안

### Q. HTML도 잘 모르는데 이걸 어떻게 했어요?

`맞습니다. 원래 HTML도 거의 모르는 상태였습니다. 그래서 이번에는 혼자 무작정 만들기보다, AI를 튜터처럼 활용해서 구조를 설명받고 이해하면서 진행했습니다. 지금은 전체 구조와 핵심 기능 흐름은 설명할 수 있는 수준까지 정리했습니다.`

### Q. AI가 다 만든 거 아닌가요?

`도움은 많이 받았습니다. 다만 그냥 결과만 가져다 쓴 게 아니라, 제 수준이 낮았기 때문에 오히려 더 구조 설명을 같이 보면서 진행했습니다. 그래서 지금은 화면이 왜 이렇게 나뉘어 있는지, 검색/수정/저장이 왜 이렇게 돌아가는지는 설명할 수 있습니다.`

### Q. 본인이 이해한 걸 하나 설명해보세요.

`예를 들면 수정 기능은 같은 폼을 재사용하는 방식입니다. 수정 버튼을 누르면 기존 데이터가 폼에 다시 채워지고, 저장할 때 editingMeetingId가 있으면 새 추가가 아니라 기존 데이터를 바꾸는 흐름입니다. 그래서 폼을 하나만 두고도 추가와 수정을 모두 처리할 수 있습니다.`

### Q. localStorage는 왜 썼나요?

`백엔드를 아직 붙이지 않은 단계라서, 사용자가 추가한 데이터가 새로고침해도 남아 있도록 하기 위해 썼습니다. 지금은 브라우저 안에 임시 저장하는 방식입니다.`

### Q. 왜 HTML/CSS/JS를 나눴나요?

`HTML은 구조, CSS는 디자인, JavaScript는 동작을 담당하게 해서 역할을 분리했습니다. 웹을 처음 배우는 입장에서도 이 방식이 가장 이해하기 쉬웠고, 수정할 때도 편합니다.`

### Q. 가장 어려웠던 부분은 뭐였나요?

`웹이 처음이라 구조를 나누는 것 자체가 어려웠습니다. 특히 단순히 화면만 만드는 게 아니라, 검색이나 수정처럼 데이터가 바뀌면 다시 화면을 그려야 한다는 흐름을 이해하는 게 가장 어려웠습니다.`

### Q. 그럼 지금 네 수준은 어느 정도예요?

`전문적으로 웹을 할 수 있는 수준은 아니고, HTML/CSS/JavaScript를 나눠서 기본 구조를 만들고, 검색/추가/수정/삭제/저장 같은 흐름을 이해하는 초중반 단계라고 보는 게 맞습니다.`

### Q. 왜 이 디자인을 선택했나요?

`처음엔 단순 뼈대 수준으로 시작했고, 이후에는 발표나 시연에서 보기 좋도록 톤을 정리했습니다. 다만 기능 구조는 그대로 두고, 디자인만 서비스 성격에 맞게 바꿨습니다.`

### Q. 배포는 누가 했나요?

`배포도 같이 정리했습니다. GitHub 저장소에 올리고 GitHub Pages로 연결해서 실제 링크로 확인할 수 있게 만들었습니다.`

---

## 7. "설명해봐"가 들어왔을 때 바로 말할 수 있는 긴 답변 예시

### 7-1. 검색 기능을 설명하라고 할 때

`검색은 사용자가 입력한 값을 읽고, 현재 저장된 모임 데이터 배열에서 조건에 맞는 것만 골라내는 방식입니다. 키워드, 카테고리, 지역, 상태를 확인해서 맞는 데이터만 남기고, 그 결과를 다시 화면에 렌더링합니다. 그리고 정렬 옵션이 있으면 날짜순이나 인기순으로 다시 정리해서 보여줍니다. 지금은 서버 검색이 아니라 프론트엔드에서 먼저 흐름을 구현한 단계입니다.`

### 7-2. 추가/수정을 설명하라고 할 때

`모임 추가와 수정은 같은 폼을 재사용합니다. 새 모임일 때는 입력값으로 새 데이터를 만들고 목록 앞에 추가합니다. 수정일 때는 수정할 모임 id를 기억해 두고, 기존 값을 폼에 채운 상태에서 저장하면 그 데이터만 바꾸는 방식입니다. 저장이 끝나면 localStorage에 다시 저장하고 화면도 다시 그립니다.`

### 7-3. 전체 구조를 설명하라고 할 때

`전체 구조는 사용자 흐름 기준으로 나눴습니다. 첫 화면에서 서비스 목적을 보여주고, 그 다음 검색으로 원하는 모임을 찾게 하고, 내 모임과 내가 만든 모임을 구분해서 보여줍니다. 그리고 마지막에는 새 모임을 추가하거나 수정할 수 있는 폼을 둡니다. 디자인은 나중에 다듬었지만, 기능 흐름은 이 구조를 유지하고 있습니다.`

### 7-4. 로그인 페이지를 설명하라고 할 때

`로그인은 메인과 역할이 다르기 때문에 별도 페이지로 분리했습니다. 현재는 API 없이 구현하는 단계라서, 실제 인증보다는 디자인과 입력 흐름을 먼저 만들었습니다. 그래서 입력, 비밀번호 보기/숨기기, 버튼 반응은 있지만 실제 세션 처리나 서버 인증은 아직 없습니다.`

---

## 8. 내가 진짜로 외워야 하는 최소 설명 8개

아래 8개는 꼭 말할 수 있어야 합니다.

1. `HTML은 구조, CSS는 디자인, JavaScript는 동작이다`
2. `검색은 입력값을 읽고 배열을 필터링해서 다시 렌더링하는 방식이다`
3. `정렬은 필터링 결과를 다시 정렬하는 단계다`
4. `추가는 새 객체를 만들어 배열에 넣고 다시 그리는 방식이다`
5. `수정은 같은 폼을 재사용하고 기존 데이터를 덮어쓰는 방식이다`
6. `삭제는 확인창 후 배열에서 제거하고 저장하는 방식이다`
7. `localStorage는 새로고침 후에도 데이터가 남게 하기 위해 쓴다`
8. `mine과 createdByMe를 나눈 이유는 참여와 작성 권한이 다르기 때문이다`

이 8개를 말할 수 있으면 기초 질문은 대부분 막을 수 있습니다.

---

## 9. 모르면 이렇게 답하기

모르는 걸 아는 척하면 더 위험합니다. 아래처럼 답하면 됩니다.

`그 부분은 처음 구현할 때 도움을 받아서 만들었고, 지금은 전체 목적과 흐름은 이해하고 있습니다. 다만 세부 문법은 아직 더 공부해야 합니다.`

또는

`지금 당장 정확한 문법 이름까지는 기억이 안 나지만, 역할은 설명드릴 수 있습니다.`

이 답변은 솔직하고, 동시에 완전히 모른다는 느낌도 줄여 줍니다.

---

## 10. 실제 발표 직전용 30초 답변

`저는 원래 웹이 처음이라 HTML도 거의 모르는 상태였습니다. 그래서 이번 웹 파트는 AI를 튜터처럼 활용해서 구조와 동작을 설명받으면서 진행했습니다. 메인 구조, 검색, 모임 추가/수정/삭제, 상세 팝업, localStorage 저장까지 먼저 프로토타입으로 구현했고, 실제 API는 다음 단계로 남겨 둔 상태입니다.`

---

## 11. 실제 발표 직전용 1분 답변

`저는 원래 웹 개발 경험이 거의 없어서 HTML도 기초부터 배우는 수준이었습니다. 그래서 이번 프로젝트에서는 AI를 단순 생성기가 아니라 튜터와 리뷰 도구처럼 활용했습니다. 화면이 왜 이렇게 나뉘는지, 검색이 왜 이런 흐름으로 도는지, 추가와 수정을 왜 같은 폼으로 처리하는지 같은 구조를 설명받으면서 이해 가능한 형태로 정리했습니다. 현재는 메인 페이지, 로그인 데모, 검색/정렬, 모임 추가/수정/삭제, 상세 팝업, localStorage 저장, GitHub Pages 배포까지 구현된 상태이고, 실제 로그인 인증이나 지도 API는 다음 단계로 남겨 두었습니다.`

---

## 12. 마지막 정리

가장 안전한 말은 이것입니다.

`나는 웹을 원래 잘 몰랐고, 그래서 AI를 튜터처럼 활용해 배우면서 만들었다. 지금은 핵심 구조와 기능 흐름은 설명할 수 있다.`

이 한 줄에서 벗어나지 않으면 됩니다.
